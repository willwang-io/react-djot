# react-djot-demo

Local React demo app for `@willwang-io/react-djot`.

## Run

From the repository root:

```bash
npm run build
cd examples/react-djot-demo
npm install
npm run dev
```

Open the URL printed by Vite (usually `http://localhost:5173`).
